standardization:
  method: "p_pos_minus_p_neg"      # score = p_pos - p_neg
  confidence: "max_prob"           # conf = max(p_pos,p_neu,p_neg)
  strength: "score_times_conf"     # strength = score * conf
